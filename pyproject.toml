[project]
name = "compiladores-g6-t01"
version = "0.1.0"
description = ""
# authors = [{ name = "Your Name", email = "you@example.com" }]
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
docs = [
    "mkdocs (<2.0.0)",
    "mkdocs-material (>=9.7.6,<10.0.0)"
]
